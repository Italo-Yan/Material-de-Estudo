// Return
// Retorna um valor
// Termina a função

/*
function soma(a, b) {
    return a + b
}

    // Não executa nada, pois a função não está retornando um valor
function soma2 (a, b) {
    console.log(a + b);
}

soma2(5,2);

// Retorna um "valor" apartir de um evento.
document,addEventListener('click', function() {
    this.document.body.style.backgroundColor = 'red';
});


function criaPessoa(nome, sobrenome) {
    return { nome, sobrenome };
};

const p1 = criaPessoa('Italo', 'Mendes');
const p2 = {
    nome: 'Yan',
    sobrenome: 'Silva'
};

console.log(p1);
console.log(p2);
*/

function duplica(n) {
    return n * 2;
}

function triplica(n) {
    return n * 3;
}

function quadriplica(n) {
    return n * 4;
}

console.log(duplica(2));
console.log(triplica(2));
console.log(quadriplica(2));